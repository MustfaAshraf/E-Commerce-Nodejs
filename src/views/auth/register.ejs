<!DOCTYPE html>

<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Register | Electro Store</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background-color: #f5f5f5;
            font-family: "Poppins", sans-serif;
        }

        .register-container {
            max-width: 450px;
            background: #fff;
            margin: 70px auto;
            padding: 35px 40px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }

        .register-header h2 {
            text-align: center;
            margin-bottom: 25px;
            color: #333;
            font-weight: 600;
        }

        .form-group {
            margin-bottom: 18px;
        }

        label {
            display: block;
            font-size: 14px;
            font-weight: 500;
            margin-bottom: 6px;
        }

        input {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 6px;
            outline: none;
        }

        input:focus {
            border-color: #ff7a00;
        }

        .register-btn {
            width: 100%;
            padding: 12px;
            background: #ff7a00;
            color: #fff;
            border-radius: 6px;
            border: none;
            cursor: pointer;
            font-weight: 500;
            font-size: 15px;
            transition: 0.2s;
        }

        .register-btn:hover {
            background: #e06900;
        }

        .extra-links {
            margin-top: 15px;
            text-align: center;
        }

        .extra-links a {
            color: #ff7a00;
            text-decoration: none;
        }
    </style>
</head>

<body>

    <div class="register-container">
        <div class="register-header">
            <h2>Create Your Account</h2>
        </div>

        <form action="/auth/register" method="POST">

            <div class="form-group">
                <label>Name</label>
                <input type="text" name="name" placeholder="Enter Full Name" required />
            </div>

            <div class="form-group">
                <label>Email Address</label>
                <input type="email" name="email" placeholder="Enter Email" required />
            </div>

            <div class="form-group">
                <label>Country</label>
                <input type="text" name="address.country" placeholder="Your Country" required />
            </div>

            <div class="form-group">
                <label>City</label>
                <input type="text" name="address.city" placeholder="Your City" required />
            </div>
            
            <div class="form-group">
                <label>Password</label>
                <input type="password" name="password" placeholder="Enter Password" required />
            </div>

            <button class="register-btn" type="submit">Register</button>
        </form>

        <div class="extra-links">
            <p>Already have an account? <a href="/login">Login here</a></p>
        </div>
    </div>

</body>

</html>