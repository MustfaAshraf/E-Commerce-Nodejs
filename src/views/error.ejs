<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Electro</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">

    <style>
        .error-page-container {
            min-height: 60vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 50px 20px;
        }
        .error-icon {
            font-size: 80px;
            color: #fed700; /* Electro Yellow */
            margin-bottom: 20px;
        }
        .error-title {
            font-size: 36px;
            font-weight: 700;
            color: #333e48;
            margin-bottom: 15px;
        }
        .error-text {
            font-size: 18px;
            color: #666;
            margin-bottom: 30px;
            max-width: 500px;
        }
        .home-btn {
            background-color: #333e48; /* Electro Dark */
            color: #fed700; /* Electro Yellow Text */
            padding: 12px 30px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            transition: all 0.3s;
        }
        .home-btn:hover {
            background-color: #fed700;
            color: #333e48;
        }
    </style>
</head>
<body>

    <%- include('layouts/navbar') %>

    <div class="container">
        <div class="error-page-container">
            <!-- Icon -->
            <div class="error-icon">
                <i class="fas fa-exclamation-triangle"></i>
            </div>
            
            <!-- Message -->
            <h1 class="error-title">Oops! Something went wrong.</h1>
            <p class="error-text">
                <%= typeof message !== 'undefined' ? message : "We couldn't process your request." %>
            </p>

            <!-- Action -->
            <a href="/" class="home-btn">
                <i class="fas fa-home me-2"></i> Back to Home
            </a>
        </div>
    </div>

    <%- include('layouts/footer') %>

</body>
</html>