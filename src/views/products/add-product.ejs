<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> - Electro</title>
    
    <!-- Assuming these match your public folder -->
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">

    <style>
        /* Electro Theme Custom Styles */
        .electro-form-container {
            background: #fff;
            padding: 40px;
            border-radius: 5px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.05);
            margin-bottom: 50px;
            margin-top: 30px;
            border-top: 3px solid #fed700; /* Electro Yellow Top Border */
        }
        .form-section-title {
            padding-bottom: 15px;
            margin-bottom: 25px;
            font-size: 24px;
            font-weight: 700;
            color: #333e48;
            border-bottom: 1px solid #eaeaea;
        }
        /* The Signature Electro Yellow Button */
        .electro-btn-primary {
            background-color: #fed700;
            color: #333;
            font-weight: 700;
            border: none;
            padding: 12px 30px;
            border-radius: 50px; 
            transition: all 0.3s;
            width: 100%;
        }
        .electro-btn-primary:hover {
            background-color: #f5cf00;
            color: #000;
            transform: translateY(-2px);
        }
        .alert-error {
            background-color: #fff6f6;
            border-left: 5px solid #d32f2f;
            color: #c62828;
            padding: 15px;
            margin-bottom: 25px;
            display: flex;
            align-items: center;
        }
        .alert-error i { margin-right: 10px; font-size: 1.2rem; }
        
        label { font-weight: 600; color: #333e48; margin-bottom: 5px; }
    </style>
</head>
<body>

    <!-- 1. Include Navbar -->
    <%- include('../layouts/navbar') %>

    <!-- 2. Breadcrumb (To match theme style) -->
    <div class="container">
        <nav aria-label="breadcrumb" style="margin-top: 20px;">
            <ol class="breadcrumb" style="background: transparent; padding-left: 0;">
                <li class="breadcrumb-item"><a href="/" style="color: #333;">Home</a></li>
                <li class="breadcrumb-item"><a href="/shop" style="color: #333;">Shop</a></li>
                <li class="breadcrumb-item active" aria-current="page"><%= pageTitle %></li>
            </ol>
        </nav>
    </div>

    <!-- 3. Main Form Section -->
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="electro-form-container">
                    
                    <h2 class="form-section-title">
                        <%= editing ? 'Edit Product' : 'Add New Product' %>
                    </h2>

                    <!-- Error Alert Display -->
                    <% if (typeof message !== 'undefined' && message) { %>
                        <div class="alert-error">
                            <i class="fas fa-exclamation-circle"></i>
                            <span><%= message %></span>
                        </div>
                    <% } %>

                    <!-- Form Start -->
                    <form 
                        action="/products/<%= editing ? 'edit/' + product._id : 'add' %>" 
                        method="POST" 
                        enctype="multipart/form-data"
                    >
                        
                        <!-- Name -->
                        <div class="mb-3">
                            <label>Product Name</label>
                            <input 
                                type="text" 
                                class="form-control" 
                                name="name" 
                                placeholder="e.g. Wireless Bluetooth Headphones"
                                value="<%= (userInput.name) ? userInput.name : '' %>"
                                required
                            >
                        </div>

                        <div class="row">
                            <!-- Price -->
                            <div class="col-md-6 mb-3">
                                <label>Price ($)</label>
                                <input 
                                    type="number" 
                                    step="0.01" 
                                    class="form-control" 
                                    name="price" 
                                    placeholder="0.00"
                                    value="<%= (userInput.price) ? userInput.price : '' %>"
                                    required
                                >
                            </div>

                            <!-- Quantity -->
                            <div class="col-md-6 mb-3">
                                <label>Stock Quantity</label>
                                <input 
                                    type="number" 
                                    class="form-control" 
                                    name="quantity" 
                                    placeholder="0"
                                    value="<%= (userInput.quantity) ? userInput.quantity : '' %>"
                                >
                            </div>
                        </div>

                        <!-- Category -->
                        <div class="mb-3">
                            <label>Category</label>
                            <select class="form-select form-control" name="category" required>
                                <option value="" disabled <%= !userInput.category ? 'selected' : '' %>>Select a Category</option>
                                
                                <% if (categories.length > 0) { %>
                                    <% categories.forEach(cat => { %>
                                        <option 
                                            value="<%= cat._id %>"
                                            <%= (userInput.category && userInput.category.toString() === cat._id.toString()) ? 'selected' : '' %>
                                        >
                                            <%= cat.name %>
                                        </option>
                                    <% }) %>
                                <% } else { %>
                                    <option disabled>No categories available</option>
                                <% } %>
                            </select>
                        </div>

                        <!-- Image Upload -->
                        <div class="mb-3">
                            <label>Product Image</label>
                            <!-- Note: If editing, file is optional -->
                            <input type="file" class="form-control" name="image" <%= editing ? '' : 'required' %>>
                            
                            <% if (editing && product.image) { %>
                                <div class="mt-3 p-2 border rounded bg-light">
                                    <small class="text-muted d-block mb-2">Current Image:</small>
                                    <img src="/uploads/productImages/<%= product.image %>" alt="Current" style="height: 100px; object-fit: contain;">
                                </div>
                            <% } %>
                        </div>

                        <!-- Description -->
                        <div class="mb-3">
                            <label>Description</label>
                            <textarea 
                                class="form-control" 
                                name="description" 
                                rows="5" 
                                placeholder="Enter full product details..."
                            ><%= (userInput.description) ? userInput.description : '' %></textarea>
                        </div>

                        <!-- Submit Button -->
                        <div class="mt-4">
                            <button type="submit" class="electro-btn-primary">
                                <i class="fas fa-save me-2"></i> 
                                <%= editing ? 'Update Product' : 'Create Product' %>
                            </button>
                        </div>

                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- 4. Include Footer -->
    <%- include('../layouts/footer') %>

</body>
</html>